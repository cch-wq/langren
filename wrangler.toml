name = "werewolf-game"
compatibility_date = "2024-09-01"
pages_build_output_dir = "dist"

# D1 绑定
[[d1_databases]]
binding = "DB"
database_name = "werewolf-game"
database_id = "44a91932-2a9c-4949-aca0-77b2ccf9e7c8"

# 密码配置：
# - 本地开发：复制 .dev.vars.example 为 .dev.vars，填入 ADMIN_PASSWORD / CREATE_PASSWORD
# - 线上：Cloudflare Pages 项目 → Settings → Environment variables 添加
#   （因为 wrangler.toml 管理绑定，密码环境变量也需要通过 dashboard 配置后重新部署）
